# TIAMAT A-axis Research Intake Index V1

This index records the newly supplied A-axis material without promoting it to runtime authority.

## Complete readable bundles

- `TIAMAT_A_AXIS_CHUG_RUNTIME_HANDOFF_20260702.tar.zst`
- `A_CHUG_POCKET_ROUTER_COURT_V0_20260702.tar.zst`
- `archiveNew.zip`

## archiveNew contents

The complete ZIP contains 16 A-axis court packages covering age sidechain, cell-conditioned CHUG, CHUG pocket/sidechain, circuit battery, current/CMRR, enter templates, event qualification, exit latch, mute/recover stability, phase impedance, and split enter/charge/exit/release research.

## Research conclusion

The packages form a progression rather than independent unrelated formulas:

```text
split law
 -> EXIT latch
 -> phase/template/circuit residual tests
 -> CHUG mute/recover
 -> age-conditioned routing
 -> cell-fuse fallback
 -> reconstructed runtime
 -> exact trace replay
```

The progression supports an architectural hypothesis: **timing/age can act as a routing coordinate that chooses among local mechanisms**, rather than being collapsed into one universal burden scalar.

This remains a hypothesis until the counterfactual court proves conditional value beyond the fixed candidate gates.
