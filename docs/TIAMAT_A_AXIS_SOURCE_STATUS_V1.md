# TIAMAT A-axis Source Status V1

The newly supplied `archiveNew.zip` is readable and contains 16 research packages. It is the preferred complete bundle for the A-axis courts inspected in this intake.

The separate `TIAMAT_A_AXIS_CHUG_RUNTIME_HANDOFF_20260702.tar.zst` is also structurally readable and contains the reconstructed runtime draft plus replay/cell-fuse packages.

The runtime package explicitly declares:

- `canon_final = false`
- `real_v10_source_used = false`
- `real_tiamat_v10_logs_used = false`

Therefore exact replay is treated as reconstruction reproducibility, not proof of original-source recovery.

The separate `A_CHUG_POCKET_ROUTER_COURT_V0_20260702.tar.zst` is a valid court package and is retained as research evidence.

## Preferred source hierarchy

1. Complete individual court packages in `archiveNew.zip`.
2. Complete A-axis runtime/replay/cell-fuse packages in the readable runtime handoff.
3. Reconstructed runtime code only as a replayable research implementation.
4. Never infer original V10 source from reconstructed packages.

## Current disposition

No A-axis family receives runtime or probability authority from this intake alone.
